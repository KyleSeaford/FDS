/* body styles */
body {
    background-color: #e6e6e6; /* Main background color (darker off-white) */
    font-family: Arial, sans-serif;
    margin: 0;
    overflow: hidden;
}

/* header styles */
header {
    background-color: #b3b3b3; /* Header background color (darker gray) */
    padding: 5px;
}

nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    width: 100%;
}

.tablink-container {
    display: flex;
}

.tablink {
    background-color: #999999; /* Nav button background color (dark gray) */
    color: #272727; /* Text color */
    cursor: pointer;
    padding: 15px 15px;
    font-size: 15px;
    border: none;
    outline: none;
    width: 110px;
    margin-right: 5px;
    transition: 0.3s;
}

.tablink:hover {
    background-color: #808080; /* Nav button hover background color (darker gray) */
}

/* button styles */
.button-container {
    display: flex;
    align-items: center;
}

.settingsButton {
    background-color: #b3b3b3; /* Settings button background color (darker gray) */
    color: #272727; /* Text color */
    cursor: help;
    padding: 15px 15px;
    font-size: 15px;
    border: none;
    outline: none;
    width: 141px;
}

.settingsButton:hover {
    background-color: #999999; /* Settings button hover background color (dark gray) */
}

.helpButton {
    background-color: #ff2200; /* Help button background color (red) */
    color: white;
    cursor: help;
    padding: 15px 15px;
    font-size: 15px;
    border: none;
    outline: none;
    width: 40px;
    margin-left: 5px; /* Add margin to separate from the settings button */
}

.helpButton:hover {
    background-color: rgba(255, 0, 0, 0.705); /* Help button hover background color (darker red) */
}

.unitButton {
    background-color: #b3b3b3; /* Unit button background color (darker gray) */
    color: #272727; /* Text color */
    cursor: help;
    padding: 15px 15px;
    font-size: 15px;
    border: none;
    outline: none;
    width: 185px; /* Adjusted width to 185px */
    margin-left: 5px; /* Add margin to separate from the settings button */
}

.unitButton:hover {
    background-color: #999999; /* Unit button hover background color (dark gray) */
}

/* info styles */
.info-bar {
    display: flex;
    background-color: #cccccc; /* Info bar background color (gray) */
}

.date-time,
.geoData {
    margin-left: 5px;
    display: flex;
    font-size: 15px;
    color: #272727; /* Text color */
}

.date-time h4,
.geoData h4 {
    margin-right: 20px;
}

.n {
    color: #272727; /* Text color */
    font-size: 15px;
    margin-left: 5px;
    margin-right: auto;
}

/* unit help styles */
.unit-container {
    position: absolute;
    top: 120px; /* Adjust the distance from the top edge */
    right: 5px; /* Adjust the distance from the right edge */
}

.unit-table {
    margin-top: 5px; /* Adjust spacing as needed */
    margin-left: 5px; /* Adjust spacing as needed */
    border-collapse: collapse;
    width: 185px; /* Change to auto to match the content width */
}

.unit-table th,
.unit-table td {
    border: 2px solid #ddd;
    padding: 10px;
    text-align: left;
    background-color: aqua;
}

.unit-table th {
    background-color: #f2f2f2;
}

/* main styles */
h1, h2, h3 {
    color: #272727; /* Text color */
    font-size: 20px;
    margin-left: 5px;
}
